name: NaïveProxy Plugin Release Build
on:
  workflow_dispatch:
    inputs:
      release:
        description: 'Release (y/n)'
        required: false
      tag:
        description: 'Release Tag'
        required: false
jobs:
  native:
    runs-on: ubuntu-latest
    name: Native Build
    strategy:
      fail-fast: false
      matrix:
        arch: [ armeabi-v7a, arm64-v8a, x86, x86_64 ]
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Checkout Submodule
        run: |
          git submodule update --init plugin/naive/src/main/jni/naiveproxy
      - name: Fetch Status
        run: git submodule status 'plugin/naive/*' > naive_status
      - name: Native Cache
        id: cache
        uses: actions/cache@v6
        with:
          path: |
            plugin/naive/src/main/jniLibs/${{ matrix.arch }}
          key: naive-v2-${{ matrix.arch }}-${{ hashFiles('bin/plugin/naive/*', 'naive_status') }}
      - name: Setup Java
        uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: 21
          cache: 'gradle'
      - name: Native Build
        run: |
          sudo apt-get update
          sudo apt-get install -y ccache
          if [ ! -s "plugin/naive/src/main/jniLibs/${{ matrix.arch }}/libnaive.so" ]; then
            ./run init action naive
            ./run plugin naive init
            ./run plugin naive ${{ matrix.arch }}
          fi
          test -s "plugin/naive/src/main/jniLibs/${{ matrix.arch }}/libnaive.so"
      - name: Upload Native
        uses: actions/upload-artifact@v7
        with:
          name: naive-${{ matrix.arch }}
          path: plugin/naive/src/main/jniLibs/${{ matrix.arch }}/libnaive.so
  build:
    name: Gradle Build
    runs-on: ubuntu-latest
    needs:
      - native
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Download Native (armeabi-v7a)
        uses: actions/download-artifact@v8
        with:
          name: naive-armeabi-v7a
          path: plugin/naive/src/main/jniLibs/armeabi-v7a
      - name: Download Native (arm64-v8a)
        uses: actions/download-artifact@v8
        with:
          name: naive-arm64-v8a
          path: plugin/naive/src/main/jniLibs/arm64-v8a
      - name: Download Native (x86)
        uses: actions/download-artifact@v8
        with:
          name: naive-x86
          path: plugin/naive/src/main/jniLibs/x86
      - name: Download Native (x86_64)
        uses: actions/download-artifact@v8
        with:
          name: naive-x86_64
          path: plugin/naive/src/main/jniLibs/x86_64
      - name: Verify Native
        run: |
          for abi in armeabi-v7a arm64-v8a x86 x86_64; do
            test -s "plugin/naive/src/main/jniLibs/$abi/libnaive.so"
          done
      - name: Gradle cache
        uses: actions/cache@v6
        with:
          path: ~/.gradle
          key: native-${{ hashFiles('**/*.gradle.kts') }}
      - name: Setup Java
        uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: 21
          cache: 'gradle'
      - name: Gradle Build
        run: |
          echo "sdk.dir=${ANDROID_HOME}" > local.properties
          export LOCAL_PROPERTIES="${{ secrets.LOCAL_PROPERTIES }}"
          ./gradlew :plugin:naive:assembleOssRelease
          APK="plugin/naive/build/outputs/apk/oss/release"
          echo "APK=$APK" >> $GITHUB_ENV
      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: APKs
          path: ${{ env.APK }}
  publish:
    name: Publish Release
    if: github.event.inputs.release == 'y' && github.event.inputs.tag != ''
    runs-on: ubuntu-latest
    needs: build
    env:
      GH_TOKEN: ${{ github.token }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          name: APKs
          path: artifacts
      - name: Build release notes
        env:
          TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          previous="$(git tag --sort=-creatordate | grep -Fxv "$TAG" | head -n1 || true)"
          repo="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
          if [ -n "$previous" ]; then
              link="${repo}/compare/${previous}...${TAG}"
          else
              link="${repo}/commits/${TAG}"
          fi
          printf '**Full Changelog**: %s\n' "$link" > release-notes.md
          cat release-notes.md
      - name: Publish Release
        run: |
          mkdir apks
          find artifacts -name "*.apk" -exec cp {} apks \;
          gh release create --latest=false "${{ github.event.inputs.tag }}" --notes-file release-notes.md --title="${{ github.event.inputs.tag }}" ./apks/*
